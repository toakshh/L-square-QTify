import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      light: '#97df99',
      main: '#34c94b',
      dark: '#006b00',
      contrastText: '#fff',
     
    }
  },
});
export default theme